// Used by `ng serve` / development builds. Swapped out for environment.prod.ts
// in production builds via the fileReplacements entry in angular.json.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5080/api',
};
