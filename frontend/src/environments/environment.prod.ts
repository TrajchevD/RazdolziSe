// Used automatically for `ng build` / `ng build --configuration production`
// (production is the default configuration for this project).
//
// TODO: replace this with your real Render backend URL once it's deployed,
// e.g. 'https://tripsplit-api.onrender.com/api'.
export const environment = {
  production: true,
  apiBaseUrl: 'https://REPLACE-WITH-YOUR-RENDER-URL.onrender.com/api',
};
